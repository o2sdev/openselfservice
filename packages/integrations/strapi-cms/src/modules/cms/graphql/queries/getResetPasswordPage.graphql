query getResetPasswordPage($locale: I18NLocaleCode!) {
    resetPasswordPage(locale: $locale) {
        ...ResetPasswordPage
    }
    configurableTexts {
        validation {
            isRequired
            isOptional
        }
    }
}
