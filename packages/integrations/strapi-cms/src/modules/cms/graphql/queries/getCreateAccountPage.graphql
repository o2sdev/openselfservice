query getCreateAccountPage($locale: I18NLocaleCode!) {
    createAccountPage(locale: $locale) {
        ...CreateAccountPage
    }
    configurableTexts {
        validation {
            isRequired
            isOptional
        }
    }
}
