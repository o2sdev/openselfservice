query getSurvey($code: String) {
    surveyJsForms(filters: { code: { eq: $code } }) {
        documentId
        code
        surveyId
        postId
        surveyType
        requiredRoles
        submitDestination
    }
}
