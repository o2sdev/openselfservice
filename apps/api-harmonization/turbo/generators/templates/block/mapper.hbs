import { CMS } from '../../models';

import { {{ pascalCase name }}Component } from './{{ kebabCase name }}.model';

export const map{{ pascalCase name }} = (
    cms: CMS.Model.{{ pascalCase name }}Component.{{ pascalCase name }}Component,
    locale: string,
): {{ pascalCase name }}Component => {
    return {
        __typename: '{{ pascalCase name }}Component',
        id: cms.id,
    };
};
