import { Injectable } from '@nestjs/common';
import { Observable, forkJoin, map } from 'rxjs';

import { CMS } from '../../models';

import { map{{ pascalCase name }} } from './{{ kebabCase name }}.mapper';
import { {{ pascalCase name }}Component } from './{{ kebabCase name }}.model';
import { Get{{ pascalCase name }}ComponentQuery } from './{{ kebabCase name }}.request';

@Injectable()
export class {{ pascalCase name }}Service {
    constructor(
        private readonly cmsService: CMS.Service,
    ) {}

    get{{ pascalCase name }}Component(query: Get{{ pascalCase name }}ComponentQuery): Observable<{{ pascalCase name }}Component> {
        const cms = this.cmsService.get{{ pascalCase name }}Component(query);

        return forkJoin([cms]).pipe(map(([cms]) => map{{ pascalCase name }}(cms, query.locale)));
    }
}
