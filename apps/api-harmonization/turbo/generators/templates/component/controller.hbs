import { Controller, Get, Query, UseInterceptors } from '@nestjs/common';

import { Auth } from '@o2s/framework/modules';
import { LoggerService } from '@o2s/utils.logger';

import { URL } from './';
import { Get{{ pascalCase name }}ComponentQuery } from './{{ kebabCase name }}.request';
import { {{ pascalCase name }}Service } from './{{ kebabCase name }}.service';

@Controller(URL)
@UseInterceptors(LoggerService)
export class {{ pascalCase name }}Controller {
    constructor(protected readonly service: {{ pascalCase name }}Service) {}

    @Get()
    @Auth.Decorators.Roles({ roles: [Auth.Constants.Roles.USER, Auth.Constants.Roles.ADMIN] })
    get{{ pascalCase name }}Component(@Query() query: Get{{ pascalCase name }}ComponentQuery) {
        return this.service.get{{ pascalCase name }}Component(query);
    }
}
