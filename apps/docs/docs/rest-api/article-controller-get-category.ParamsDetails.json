{
    "parameters": [
        {
            "name": "id",
            "required": true,
            "in": "path",
            "description": "Article category identifier.",
            "schema": { "type": "string" }
        },
        {
            "name": "locale",
            "required": false,
            "in": "query",
            "description": "Optional locale code used to localize response.",
            "schema": { "type": "string" }
        }
    ]
}
