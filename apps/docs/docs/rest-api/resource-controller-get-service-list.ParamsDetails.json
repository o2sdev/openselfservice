{
    "parameters": [
        {
            "name": "status",
            "required": false,
            "in": "query",
            "description": "Filter services by contract status.",
            "schema": { "type": "string", "enum": ["ACTIVE", "EXPIRED", "INACTIVE"] }
        },
        {
            "name": "type",
            "required": false,
            "in": "query",
            "description": "Filter services by product type.",
            "schema": { "type": "string", "enum": ["PHYSICAL", "VIRTUAL"] }
        },
        {
            "name": "category",
            "required": false,
            "in": "query",
            "description": "Filter services by category identifier.",
            "schema": { "type": "string" }
        },
        {
            "name": "billingAccountId",
            "required": false,
            "in": "query",
            "description": "Filter services by billing account identifier.",
            "schema": { "type": "string" }
        },
        {
            "name": "dateFrom",
            "required": false,
            "in": "query",
            "description": "Include services created/updated from this date (ISO string).",
            "schema": { "type": "string" }
        },
        {
            "name": "dateTo",
            "required": false,
            "in": "query",
            "description": "Include services created/updated until this date (ISO string).",
            "schema": { "type": "string" }
        },
        {
            "name": "sort",
            "required": false,
            "in": "query",
            "description": "Sort expression, for example `createdAt_DESC`.",
            "schema": { "type": "string" }
        },
        {
            "name": "locale",
            "required": false,
            "in": "query",
            "description": "Optional locale code used to localize response values.",
            "schema": { "type": "string" }
        },
        {
            "name": "query",
            "required": false,
            "in": "query",
            "description": "Service list filters and pagination query.",
            "schema": { "type": "string" }
        }
    ]
}
