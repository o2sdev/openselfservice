{
    "parameters": [
        {
            "name": "id",
            "required": true,
            "in": "path",
            "description": "Product identifier.",
            "schema": { "type": "string" }
        },
        {
            "name": "variantId",
            "required": false,
            "in": "path",
            "description": "Optional variant identifier.",
            "schema": { "type": "string" }
        },
        {
            "name": "locale",
            "required": false,
            "in": "path",
            "description": "Optional locale code used to localize response values.",
            "schema": { "type": "string" }
        },
        {
            "name": "basePath",
            "required": false,
            "in": "path",
            "description": "Optional base path override used by integration routing.",
            "schema": { "type": "string" }
        },
        {
            "name": "variantOptionGroups",
            "required": false,
            "in": "path",
            "description": "Configuration of variant option groups used by product details views.",
            "schema": { "type": "array", "items": { "type": "string" } }
        }
    ]
}
