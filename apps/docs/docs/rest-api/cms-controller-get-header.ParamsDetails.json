{
    "parameters": [
        {
            "name": "preview",
            "required": false,
            "in": "query",
            "description": "When true, returns draft/preview CMS content.",
            "schema": { "type": "boolean" }
        },
        {
            "name": "id",
            "required": true,
            "in": "query",
            "description": "Header content identifier.",
            "schema": { "type": "string" }
        },
        {
            "name": "locale",
            "required": true,
            "in": "query",
            "description": "Locale code used to localize the response, for example `pl` or `en`.",
            "schema": { "type": "string" }
        },
        { "name": "query", "required": false, "in": "query", "schema": { "type": "string" } }
    ]
}
