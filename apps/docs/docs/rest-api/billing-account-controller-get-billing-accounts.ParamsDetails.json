{
    "parameters": [
        {
            "name": "status",
            "required": false,
            "in": "query",
            "description": "Billing account status filter.",
            "schema": { "type": "string" }
        },
        {
            "name": "number",
            "required": false,
            "in": "query",
            "description": "Billing account number filter.",
            "schema": { "type": "string" }
        },
        {
            "name": "query",
            "required": false,
            "in": "query",
            "description": "Billing account list filters and pagination query.",
            "schema": { "type": "string" }
        }
    ]
}
