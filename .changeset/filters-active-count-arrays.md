---
'@o2s/ui': patch
---

fix(ui): count multi-value list filters correctly in the filters drawer, so clearing every option no longer leaves the "remove filters" counter at one and equal selections are compared by contents instead of by reference
