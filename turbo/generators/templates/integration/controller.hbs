import { Injectable } from '@nestjs/common';

{{#if (eq module 'cms')}}
    import { CMS } from '@o2s/framework/modules';
{{else}}
    import {
    {{pascalCase module}}
    } from '@o2s/framework/modules';
{{/if}}

@Injectable()
{{#if (eq module 'cms')}}
    export class
    {{pascalCase module}}Controller extends CMS.Controller {
{{else}}
    export class
    {{pascalCase module}}Controller extends
    {{pascalCase module}}.Controller {
{{/if}}
}